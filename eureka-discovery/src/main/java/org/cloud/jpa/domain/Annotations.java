package org.cloud.jpa.domain;

/*
import javax.persistence.*;

@Entity
@Table(name = "zipkin_annotations")
public class Annotations extends AbstractPersistable {

    @ManyToOne(fetch = FetchType.EAGER)
    @JoinColumn(name = "COMPONENT_ID")
    private Spans spans;
}
*/