package org.cloud.jpa.repository;

/*
import org.cloud.jpa.domain.Revision;
import org.cloud.jpa.domain.Spans;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;

import java.util.List;


public interface SpansRepository extends JpaRepository<Spans, Long> {
    @Query("SELECT s FROM ModeledRelation  s where validTo is null")
    public List<Spans> findCurrentModeledRelations();
}
*/